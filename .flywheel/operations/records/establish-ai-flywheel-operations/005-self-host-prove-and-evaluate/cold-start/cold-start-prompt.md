# AI Flywheel Context-Free Certification Startup

Use the GitHub repository `Infoconex/ai-flywheel-cli-python` at immutable revision `21ca40b7e63106bf525f0f334bb81328f38d3cff` as the only source of repository context.

Begin at the repository root. Discover and follow the repository's operator startup instructions exactly. Do not use prior chat context, cached copies, assumptions, or information outside that immutable repository revision.

Perform only the context-free startup certification scenario:

1. Discover the canonical startup entrypoint through the repository-defined startup chain.
2. Read only the files required by that chain to determine the persisted operating state.
3. Produce the required opening report.
4. Determine whether the persisted execution must be created, resumed, blocked, or left unchanged.
5. Stop after reporting the next authorized action.

Do not perform broad repository inspection, implementation validation, mutation, certification of other scenarios, readiness transition, branch merge, or application mission work.

Your report must identify:

- Operating Validation
- Repository Validation
- Implementation Validation
- Active Mission
- Active Goal
- Active Execution
- Lifecycle Stage
- Readiness
- Next Authorized Action
- Whether any mutation was performed
